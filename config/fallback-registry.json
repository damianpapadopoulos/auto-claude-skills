{
  "version": "3.2.0-fallback",
  "description": "Static fallback registry for degraded environments (no jq, missing plugin dirs)",
  "warnings": [
    "Using static fallback registry — dynamic discovery unavailable"
  ],
  "skills": [
    {
      "name": "systematic-debugging",
      "role": "process",
      "phase": "DEBUG",
      "triggers": ["debug", "bug", "fix", "broken", "error", "crash", "failing", "not working", "regression"],
      "trigger_mode": "regex",
      "priority": 50,
      "invoke": "Skill(superpowers:systematic-debugging)",
      "available": true,
      "enabled": true,
      "description": "Methodically isolate and fix bugs using structured investigation"
    },
    {
      "name": "executing-plans",
      "role": "process",
      "phase": "IMPLEMENT",
      "triggers": ["execute plan", "implement plan", "follow the plan", "next task", "continue", "pick up"],
      "trigger_mode": "regex",
      "priority": 35,
      "invoke": "Skill(superpowers:executing-plans)",
      "available": true,
      "enabled": true,
      "description": "Systematically implement a confirmed plan, task by task"
    },
    {
      "name": "brainstorming",
      "role": "process",
      "phase": "DESIGN",
      "triggers": ["brainstorm", "design", "architect", "scope", "outline", "approach", "how should"],
      "trigger_mode": "regex",
      "priority": 30,
      "invoke": "Skill(superpowers:brainstorming)",
      "available": true,
      "enabled": true,
      "description": "Explore ideas, ask clarifying questions, get design approval before building"
    },
    {
      "name": "requesting-code-review",
      "role": "process",
      "phase": "REVIEW",
      "triggers": ["review", "code review", "pull request", "pr", "check changes", "diff"],
      "trigger_mode": "regex",
      "priority": 25,
      "invoke": "Skill(superpowers:requesting-code-review)",
      "available": true,
      "enabled": true,
      "description": "Prepare and request a structured code review"
    },
    {
      "name": "test-driven-development",
      "role": "process",
      "phase": "IMPLEMENT",
      "triggers": ["tdd", "test first", "write tests", "test driven", "red green refactor"],
      "trigger_mode": "regex",
      "priority": 20,
      "invoke": "Skill(superpowers:test-driven-development)",
      "available": true,
      "enabled": true,
      "description": "Write tests first, then implement to make them pass"
    },
    {
      "name": "verification-before-completion",
      "role": "workflow",
      "phase": "SHIP",
      "triggers": ["verify", "final check", "ready to ship", "all green", "all passing", "lgtm"],
      "trigger_mode": "regex",
      "priority": 20,
      "invoke": "Skill(superpowers:verification-before-completion)",
      "available": true,
      "enabled": true,
      "description": "Run final checks before declaring work complete"
    },
    {
      "name": "writing-plans",
      "role": "process",
      "phase": "PLAN",
      "triggers": ["plan", "break down", "task list", "steps", "outline tasks", "spec"],
      "trigger_mode": "regex",
      "priority": 40,
      "invoke": "Skill(superpowers:writing-plans)",
      "available": true,
      "enabled": true,
      "description": "Break work into structured tasks and confirm the plan before execution"
    },
    {
      "name": "finishing-a-development-branch",
      "role": "workflow",
      "phase": "SHIP",
      "triggers": [],
      "trigger_mode": "regex",
      "priority": 19,
      "invoke": "Skill(superpowers:finishing-a-development-branch)",
      "available": true,
      "enabled": true,
      "description": "Clean up, squash, and prepare a branch for merge"
    }
  ]
}
