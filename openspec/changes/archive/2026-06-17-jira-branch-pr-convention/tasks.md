# Tasks: Jira Branch + PR Naming Convention

## Completed

- [x] 1.1 Atlassian MCP availability detection in `session-start-hook.sh` + tests (commit `5cb1379`)
- [x] 1.2 `jira-branch-convention` methodology hint + regex fixtures (commit `5fdaf4c`)
- [x] 1.3 SHIP-phase PR-title hint + both-directions gate test (commit `1ea008c`)
- [x] 1.4 Regenerate `fallback-registry.json` in sync (folded into `1ea008c`; in-sync test passes)
